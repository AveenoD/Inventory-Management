/** Placeholder paths for Firebase static export — real routes use client-side navigation. */
export function firebaseYearMonthParams() {
  return [{ year: "2026", month: "06" }];
}

export function firebaseInventoryIdParams() {
  return [{ id: "_" }];
}
