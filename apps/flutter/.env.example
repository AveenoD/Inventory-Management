# Same API as web (NEXT_PUBLIC_API_URL) and Expo (EXPO_PUBLIC_API_URL).
#
# Production (default for installed APK):
API_URL=https://sk-mobile-api.onrender.com
#
# Local API on Android emulator (host machine):
# API_URL=http://10.0.2.2:4000
#
# Local API on physical device (use your PC LAN IP):
# API_URL=http://192.168.1.42:4000
